import { extent } from 'dummy/helpers/extent';
import { module, test } from 'qunit';

module('Unit | Helper | extent');

// Replace this with your real tests.
test('it works', function(assert) {
  const result = extent([42]);
  assert.ok(result);
});

